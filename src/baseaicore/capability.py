"""Domain module reserved for Phase 4 — deliberately empty until then.

Phase 4 of ``docs/packages/baseaicore/development-plan.md`` adds ``CapabilityId`` — the validated
capability identifier type. The vocabulary's *contents* and their version live in SetSpec, not
here.

The file exists so the package layout is fixed before the phase that fills it, and so a
reader looking for the type knows where it will be rather than where it might go.
"""
