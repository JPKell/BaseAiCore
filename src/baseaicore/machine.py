"""Domain module reserved for Phase 3 — deliberately empty until then.

Phase 3 of ``docs/packages/baseaicore/development-plan.md`` adds ``GpuVendor``, ``GpuProfile``,
``StorageDevice``, ``MachineProfile`` and ``compute_machine_fingerprint`` — a machine identity
that survives a driver upgrade.

The file exists so the package layout is fixed before the phase that fills it, and so a
reader looking for the type knows where it will be rather than where it might go.
"""
